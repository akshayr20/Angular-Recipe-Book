import { DropwdownDirective } from './dropwdown.directive';

describe('DropwdownDirective', () => {
  it('should create an instance', () => {
    const directive = new DropwdownDirective();
    expect(directive).toBeTruthy();
  });
});
